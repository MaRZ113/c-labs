Мы вынесли математику в функции add_matrices и mult_matrices, как и требовалось в задании.

В main мы сначала генерируем матрицы a и b, записываем их в input.txt.

Чтобы выполнить условие "Очистить массивы", мы жестко вызываем free_matrix для обеих матриц, удаляя их из оперативной памяти.

Затем мы открываем input.txt для чтения, снова просим память под матрицы через malloc и заполняем их считанными из файла числами.

Проводим вычисления, результат отправляем в output.txt и в конце выводим время работы программы в консоль.